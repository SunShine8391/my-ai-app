import { Route } from "next";

export const routes = {
	home: "/" as Route<string>,
	overview: "/overview" as Route<string>,
	aiTool: "/tools" as Route<string>,
	profile: "/profile" as Route<string>,
};
